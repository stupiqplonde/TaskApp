# Todo App - FastAPI + SQLite + JavaScript

![Todo App Screenshot](https://via.placeholder.com/800x450/4a6fa5/ffffff?text=Todo+App+Preview)
*Современное, адаптивное приложение для управления задачами на FastAPI с фронтендом на JavaScript*

## 🚀 Возможности

### 📋 Основные функции
- ✅ **Создание задач** с названием, описанием, категорией и приоритетом
- ✅ **Редактирование задач** - изменение любых деталей
- ✅ **Удаление задач** - удаление отдельных задач
- ✅ **Отметка выполнения** - переключение статуса задачи
- ✅ **Фильтрация задач** - просмотр всех, активных или завершенных задач
- ✅ **Поиск задач** - поиск по названию и описанию в реальном времени
- ✅ **Сортировка по приоритету** - индикаторы высокого, среднего и низкого приоритета
- ✅ **Категории** - организация задач по пользовательским категориям
- ✅ **Статистика** - статистика выполнения задач в реальном времени

### 🎨 Интерфейс и UX
- 🎯 **Адаптивный дизайн** - работает на ПК, планшетах и мобильных устройствах
- 🎨 **Чистый, современный интерфейс** с интуитивными элементами управления
- 🌈 **Визуальные индикаторы приоритета** (цветные границы)
- 📱 **Мобильная оптимизация** навигации и управления
- 🔔 **Система уведомлений** для обратной связи с пользователем
- 📊 **Панель статистики** в реальном времени

## 🛠 Технологический стек

### Бэкенд
- **FastAPI** - Современный, быстрый веб-фреймворк для создания API
- **SQLAlchemy** - Инструментарий SQL и ORM
- **SQLite** - Легковесная база данных (не требует внешней БД)
- **Pydantic** - Валидация данных и управление настройками
- **Uvicorn** - ASGI сервер для FastAPI

### Фронтенд
- **Vanilla JavaScript** - Без фреймворков, чистый JavaScript
- **HTML5** - Семантическая разметка
- **CSS3** - Пользовательские стили с CSS переменными
- **Font Awesome** - Библиотека иконок
- **Fetch API** - Для взаимодействия с API

## 📁 Структура проекта

```
todo-app/
├── app/
│   ├── __init__.py
│   ├── main.py          # FastAPI приложение и эндпоинты
│   ├── database.py      # Конфигурация базы данных и управление сессиями
│   ├── models.py        # SQLAlchemy модели (Task)
│   ├── schemas.py       # Pydantic схемы для валидации
│   └── crud.py          # CRUD операции для задач
├── static/
│   ├── css/
│   │   └── style.css    # Все стили приложения
│   └── js/
│       └── app.js       # Логика фронтенда на JavaScript
├── templates/
│   └── index.html       # Основной HTML шаблон
├── requirements.txt     # Python зависимости
├── run.py              # Запуск приложения
└── README.md           # Этот файл
```

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <url-репозитория>
cd todo-app
```

2. **Создайте виртуальное окружение (рекомендуется)**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Запустите приложение**
```bash
python run.py
```

5. **Откройте браузер**
Перейдите по адресу `http://localhost:8000`

## 📊 Документация API

Приложение предоставляет RESTful API со следующими эндпоинтами:

### Эндпоинты для задач
| Метод | Эндпоинт | Описание | Тело запроса |
|--------|----------|-------------|--------------|
| GET | `/api/tasks/` | Получить все задачи | Параметры: `completed`, `category`, `search` |
| GET | `/api/tasks/{id}` | Получить конкретную задачу | - |
| POST | `/api/tasks/` | Создать новую задачу | `{"title": "...", "description": "...", "category": "...", "priority": 1-3}` |
| PUT | `/api/tasks/{id}` | Обновить задачу | Частичные данные задачи |
| DELETE | `/api/tasks/{id}` | Удалить задачу | - |
| PATCH | `/api/tasks/{id}/toggle` | Переключить статус выполнения | - |

### Вспомогательные эндпоинты
| Метод | Эндпоинт | Описание |
|--------|----------|-------------|
| GET | `/api/categories/` | Получить все категории |
| GET | `/api/stats/` | Получить статистику задач |

### Интерактивная документация API
После запуска приложения доступны:
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

## 🎯 Руководство по использованию

### Создание задач
1. Заполните название задачи (обязательно)
2. Добавьте описание (необязательно)
3. Выберите категорию (по умолчанию: "общее")
4. Установите приоритет (Высокий/Средний/Низкий)
5. Нажмите "Добавить задачу"

### Управление задачами
- **Завершить задачу**: Нажмите на чекбокс рядом с задачей
- **Редактировать задачу**: Нажмите на иконку карандаша (✏️)
- **Удалить задачу**: Нажмите на иконку корзины (🗑️)
- **Поиск**: Используйте поле поиска для фильтрации задач
- **Фильтр**: Используйте кнопки для отображения Всех/Активных/Завершенных задач
- **Фильтр по категориям**: Нажмите на любую категорию в боковой панели

### Приоритеты задач
- 🔴 **Высокий (1)**: Красная граница - Срочные задачи
- 🟡 **Средний (2)**: Желтая граница - Важные, но не срочные
- 🔵 **Низкий (3)**: Синяя граница - Можно сделать позже

## 🔧 Конфигурация

### База данных
Приложение по умолчанию использует SQLite. Файл базы данных (`todo.db`) автоматически создается в корне проекта.

### Настройка
Вы можете настроить приложение, изменив:

1. **Категории**: Отредактируйте опции категорий в `templates/index.html`
2. **Цвета**: Измените CSS переменные в `static/css/style.css`
3. **Уровни приоритета**: Обновите обработку приоритетов в `app/models.py` и фронтенде

## 📱 Адаптивный дизайн

Приложение полностью адаптивно и работает на:

- **ПК** (≥1024px): Боковая панель + основной контент
- **Планшет** (768px-1024px): Оптимизированные отступы и элементы управления
- **Мобильные устройства** (<768px): Вертикальная компоновка, удобные для касания кнопки

## 🧪 Тестирование API

### Использование cURL
```bash
# Получить все задачи
curl http://localhost:8000/api/tasks/

# Создать новую задачу
curl -X POST http://localhost:8000/api/tasks/ \
  -H "Content-Type: application/json" \
  -d '{"title": "Купить продукты", "category": "покупки", "priority": 1}'

# Переключить статус задачи
curl -X PATCH http://localhost:8000/api/tasks/1/toggle
```

### Использование Python
```python
import requests

# Получить задачи
response = requests.get("http://localhost:8000/api/tasks/")
tasks = response.json()

# Создать задачу
new_task = {
    "title": "Изучить FastAPI",
    "description": "Изучить документацию FastAPI",
    "category": "обучение",
    "priority": 2
}
response = requests.post("http://localhost:8000/api/tasks/", json=new_task)
```

## 🐛 Устранение неполадок

### Частые проблемы

1. **Порт уже используется**
```bash
# Завершить процесс на порту 8000
lsof -ti:8000 | xargs kill -9
```

2. **Ошибки "Модуль не найден"**
```bash
# Переустановите зависимости
pip install -r requirements.txt
```

3. **Проблемы с базой данных**
```bash
# Удалите и пересоздайте базу данных
rm todo.db
python run.py
```

### Логи
Проверьте терминал, где вы запустили `python run.py`, на наличие сообщений об ошибках и отладочной информации.

## 📈 Статистика проекта

- **Бэкенд**: ~200 строк Python
- **Фронтенд**: ~300 строк JavaScript
- **CSS**: ~400 строк
- **Всего**: ~900 строк кода
- **Зависимости**: 5 Python пакетов
- **Время настройки**: < 5 минут

## 🎨 Настройка

### Изменение цветов
Отредактируйте CSS переменные в `static/css/style.css`:
```css
:root {
    --primary-color: #4a6fa5;    /* Основной цвет */
    --success-color: #28a745;    /* Завершенные задачи */
    --danger-color: #dc3545;     /* Высокий приоритет */
    --warning-color: #ffc107;    /* Средний приоритет */
}
```

### Добавление новых функций

1. **Новые поля задач**: Добавьте столбцы в `models.py`, обновите схемы и формы
2. **Аутентификация пользователей**: Добавьте модель User и эндпоинты аутентификации
3. **Сроки выполнения**: Добавьте поля datetime для дедлайнов
4. **Общий доступ к задачам**: Добавьте функциональность общего доступа между пользователями
5. **Экспорт/Импорт**: Добавьте функциональность экспорта в JSON/CSV

## 🤝 Участие в разработке

1. Сделайте форк репозитория
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Добавлена замечательная фича'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект лицензирован под MIT License - смотрите файл LICENSE для деталей.

## 🙏 Благодарности

- [FastAPI](https://fastapi.tiangolo.com/) за отличный веб-фреймворк
- [SQLAlchemy](https://www.sqlalchemy.org/) за ORM
- [Font Awesome](https://fontawesome.com/) за иконки
- Всем контрибьюторам и пользователям этого проекта

## 📞 Поддержка

Для получения поддержки, пожалуйста:
1. Проверьте раздел [Устранение неполадок](#🐛-устранение-неполадок)
2. Создайте issue в репозитории GitHub
3. Напишите на email: [ваш-email@example.com]

---

**Создано с ❤️ на FastAPI и vanilla JavaScript**

*Удачного управления задачами!* 🎯
